# GameExchange Android

This is an Android wrapper for the current GameExchange prototype.

The included GitHub Actions workflow builds an installable debug APK in the cloud.
Real payments are not enabled. The current app is a prototype and stores demo behavior locally; multi-user cloud accounts/data still need to be connected before production launch.
